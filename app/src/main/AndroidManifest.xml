<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jcltrucks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JCLTrucks">
        <activity android:name=".ui.Gerente.Gerente"></activity>
        <activity
            android:name=".TelaGerente"
            android:label="@string/title_activity_tela_gerente" />
        <activity
            android:name=".ui.Funcionario.Funcionario"
            android:label="@string/title_activity_funcionario" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>